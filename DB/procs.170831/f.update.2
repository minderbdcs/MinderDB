COMMIT WORK;
SET AUTODDL OFF;
SET TERM ^ ;


CREATE OR ALTER TRIGGER MOD_SSN_TYPE_MIRROR FOR SSN_TYPE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PURCHASE_ORDER_MIRROR FOR PURCHASE_ORDER
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PURCHASE_ORDER_LINE_MIRROR FOR PURCHASE_ORDER_LINE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^


SET TERM ; ^
COMMIT WORK;
SET AUTODDL ON;

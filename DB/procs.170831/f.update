COMMIT WORK;
SET AUTODDL OFF;
SET TERM ^ ;


CREATE OR ALTER TRIGGER MOD_SSN_MIRROR FOR SSN 
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_ISSN_MIRROR FOR ISSN 
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_GRN_MIRROR FOR GRN 
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_GRN_ORDER_MIRROR FOR GRN_ORDER 
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_ORDER_MIRROR FOR PICK_ORDER 
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^


CREATE OR ALTER TRIGGER MOD_PICK_ITEM_LINE_NO_MIRROR FOR PICK_ITEM_LINE_NO
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_ITEM_MIRROR FOR PICK_ITEM
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_ITEM_DETAIL_MIRROR FOR PICK_ITEM_DETAIL
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_ORDER_IMPORT_MIRROR FOR PICK_ORDER_IMPORT
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_INVOICE_MIRROR FOR PICK_INVOICE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_INVOICE_LINE_MIRROR FOR PICK_INVOICE_LINE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_DESPATCH_MIRROR FOR PICK_DESPATCH
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_MANIFEST_MIRROR FOR PICK_MANIFEST
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PACK_ID_MIRROR FOR PACK_ID
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PACK_SSCC_MIRROR FOR PACK_SSCC
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^


CREATE OR ALTER TRIGGER MOD_PICK_ITEM_ADDRESS_MIRROR FOR PICK_ITEM_ADDRESS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_TRANSACTIONS4_MIRROR FOR TRANSACTIONS4
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_ISSN_DX_MIRROR FOR ISSN_DX
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_SSN_DX_MIRROR FOR SSN_DX
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_SSN_HIST_MIRROR FOR SSN_HIST
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PACK_ID_DX_MIRROR FOR PACK_ID_DX
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_ITEM_DX_MIRROR FOR PICK_ITEM_DX
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_DESPATCH_DX_MIRROR FOR PICK_DESPATCH_DX
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_ITEM_DETAIL_DX_MIRROR FOR PICK_ITEM_DETAIL_DX
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_PICK_ORDER_DX_MIRROR FOR PICK_ORDER_DX
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_GRN_DX_MIRROR FOR GRN_DX 
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_TRANSACTIONS_ARCHIVE_MIRROR FOR TRANSACTIONS_ARCHIVE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^

CREATE OR ALTER TRIGGER MOD_TRANSACTIONS4_ARCHIVE_MRR FOR TRANSACTIONS4_ARCHIVE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      /* currently mirror data is populated */
      NEW.MIRROR_DATA = NULL;
   END

END ^


SET TERM ; ^
COMMIT WORK;
SET AUTODDL ON;

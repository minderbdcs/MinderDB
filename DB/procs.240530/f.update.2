COMMIT WORK;
SET AUTODDL OFF;
SET TERM ^ ;

    

CREATE OR ALTER TRIGGER MOD_ISSN_ATTRIBUTES_MIRROR FOR ISSN_ATTRIBUTES
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PRODUCT_ATTRIBUTES_MIRROR FOR PRODUCT_ATTRIBUTES
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_LEGACY_MIRROR FOR LEGACY
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_GENERIC_MIRROR FOR GENERIC
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_GLOBAL_CONDITIONS_MIRROR FOR GLOBAL_CONDITIONS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_MOVE_STATUS_MIRROR FOR MOVE_STATUS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_OPTIONS_MIRROR FOR OPTIONS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PARAM_MIRROR FOR PARAM
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PERSON_MIRROR FOR PERSON
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^


CREATE OR ALTER TRIGGER MOD_PERSON_ADDRESS_MIRROR FOR PERSON_ADDRESS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PRINT_REQUESTS_MIRROR FOR PRINT_REQUESTS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PRINT_REQUESTS_ARCHIVE_MRR FOR PRINT_REQUESTS_ARCHIVE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PRODUCT_CONDITION_MIRROR FOR PRODUCT_CONDITION
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PRODUCT_COND_STATUS_MIRROR FOR PRODUCT_COND_STATUS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PROD_EAN_MIRROR FOR PROD_EAN
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PROD_PROFILE_MIRROR FOR PROD_PROFILE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PROD_SUPPLIER_MIRROR FOR PROD_SUPPLIER
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_REPORTS_MIRROR FOR REPORTS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_REPORTS_DETAIL_MIRROR FOR REPORTS_DETAIL
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_REPORTS_SCHEDULE_MIRROR FOR REPORTS_SCHEDULE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_REPORTS_TYPE_MIRROR FOR REPORTS_TYPE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_TRANSACTIONS_MIRROR FOR TRANSACTIONS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^


CREATE OR ALTER TRIGGER MOD_TRANSACTIONS_ADMIN_MIRROR FOR TRANSACTIONS_ADMIN
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_WEB_REQUESTS_MIRROR FOR WEB_REQUESTS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_WEB_REQUESTS_ARCHIVE_MIRROR FOR WEB_REQUESTS_ARCHIVE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_SSN_SUB_TYPE_MIRROR FOR SSN_SUB_TYPE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_SSN_TEST_MIRROR FOR SSN_TEST
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_SESSION_MIRROR FOR SESSION
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_KIT_MIRROR FOR KIT
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_PRODUCT_KIT_MIRROR FOR PRODUCT_KIT
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

    

CREATE OR ALTER TRIGGER MOD_SSN_TEST_RESULTS_MIRROR FOR SSN_TEST_RESULTS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^


CREATE OR ALTER TRIGGER MOD_LOG_MIRROR FOR LOG
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^

SET TERM ; ^
COMMIT WORK;
SET AUTODDL ON;

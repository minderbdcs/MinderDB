COMMIT WORK;
SET AUTODDL OFF;
SET TERM ^ ;

/*
CREATE OR ALTER PROCEDURE ADD_PICK_ITEM_LINE_NO (PICK_ORDER VARCHAR(15) ,
*/

CREATE OR ALTER PROCEDURE ADD_PICK_ITEM_LINE_NO (PICK_ORDER PICK_ORDER ,
LAST_LINE_NO INTEGER)
AS 
BEGIN
  /* INSERT INTO PICK_ITEM_LINE_NO (PICK_ORDER, LAST_LINE_NO) VALUES (:PICK_ORDER, :LAST_LINE_NO); */
  UPDATE OR INSERT INTO PICK_ITEM_LINE_NO (PICK_ORDER, LAST_LINE_NO) VALUES (:PICK_ORDER, :LAST_LINE_NO);
END ^

SET TERM ; ^
COMMIT WORK;
SET AUTODDL ON;

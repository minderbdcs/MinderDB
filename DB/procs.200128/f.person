COMMIT;

/*
ALTER DOMAIN POST_CODE TYPE VARCHAR(20);
this has unique indexes or foreign indexes  or primarys 
*/
ALTER DOMAIN MAIL_POST_CODE TYPE VARCHAR(20);
ALTER DOMAIN DESPATCH_POST_CODE TYPE VARCHAR(20);

COMMIT;

add locn_short_name name to location

OPTIONS.GROUP_CODE = LOCATION_HR',
CODE = 'FH' and
DESCRIPTION = "'['||WH_ID||'-'||SUBSTRING(LOCN_ID FROM 1 FOR 2)||'-'||SUBSTRING(LOCN_ID FROM 3 FOR 2)||'-'||SUBSTRING(LOCN_ID FROM 5 FOR 2)||'-'||SUBSTRING(LOCN_ID FROM 7 FOR 2)||']'"

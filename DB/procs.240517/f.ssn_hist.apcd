execute  PROCEDURE MOD_SSN_HIST_RECORD ;
commit;
delete from ssn_hist where record_id in (select sh.record_id from ssn_hist sh group by sh.record_id having count(*) > 1);
commit;
ALTER TABLE SSN_HIST ADD PRIMARY KEY (RECORD_ID);

COMMIT WORK;
SET AUTODDL OFF;
SET TERM ^ ;
/*
TRANSFER_REQUEST
LEGACY_ADJUSTMENT
CHART_OF_ACCOUNTS
STOCKTAKE
VARIANCE */    

CREATE OR ALTER TRIGGER MOD_TRANSFER_REQUEST_MIRROR FOR TRANSFER_REQUEST
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^


CREATE OR ALTER TRIGGER MOD_LEGACY_ADJUSTMENT_MIRROR FOR LEGACY_ADJUSTMENT
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^


CREATE OR ALTER TRIGGER MOD_CHART_OF_ACCOUNTS_MIRROR FOR CHART_OF_ACCOUNTS
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^
    

CREATE OR ALTER TRIGGER MOD_STOCKTAKE_MIRROR FOR STOCKTAKE
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^
    

CREATE OR ALTER TRIGGER MOD_VARIANCE_MIRROR FOR VARIANCE 
ACTIVE BEFORE UPDATE POSITION 4 
AS
BEGIN
   IF ( OLD.MIRROR_DATA IS NOT NULL) THEN
   BEGIN
      NEW.MIRROR_DATA = NULL;
   END

END ^
    

SET TERM ; ^
COMMIT WORK;
SET AUTODDL ON;

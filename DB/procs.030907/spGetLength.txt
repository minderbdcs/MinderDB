CREATE PROCEDURE GET_LENGTH  (
  DATA_ID VARCHAR(20)
)
RETURNS (
  MAX_LENGTH INTEGER
) AS     
     
BEGIN

  SELECT MAX_LENGTH
  FROM PARAM
  WHERE DATA_ID = :DATA_ID
  INTO :MAX_LENGTH;
  
END^
